apiVersion: v1
kind: PersistentVolume
metadata:
  name: kind-local-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /data/persistent    # ← matches containerPath in kind-config.yaml